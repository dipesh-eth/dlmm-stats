version: '3.8'

services:
  meteora-bot:
    build:
      context: .
      dockerfile: dockerfile
    container_name: meteora-discord-bot
    restart: unless-stopped
    environment:
      # Discord Configuration
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - CLIENT_ID=${CLIENT_ID}
      # Meteora DLMM Data API
      - METEORA_API_BASE=${METEORA_API_BASE:-https://dlmm.datapi.meteora.ag}
      # Helius RPC
      - HELIUS_API_KEY=${HELIUS_API_KEY}
      # Execution / wallet encryption
      - ENCRYPTION_SECRET_KEY=${ENCRYPTION_SECRET_KEY}
      - RPC_ENDPOINT=${RPC_ENDPOINT}
      # Node Environment
      - NODE_ENV=production
    # Optional: Mount logs directory
    volumes:
      - ./logs:/app/logs
      - ./wallets.json:/app/wallets.json
      - ./bot-database.db:/app/bot-database.db
    # Resource limits (adjust as needed)
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    # Network configuration (optional)
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
